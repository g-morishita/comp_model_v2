# comp_model

Computational modeling toolkit with event-based task semantics.

For a structured, end-to-end usage guide, start with
[example/README.md](example/README.md).

The workflow examples can be run for each public model, for example:
`uv run python example/complete_workflow/03_fit_with_mle.py --model social_rl_demo_reward`.
